#include <stdio.h>

/**
 * main - print the product of the arguments
 * @argc: int
 * @argv: list
 *
 *
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int c = argv[1] * argv[2];

	if (argc == 3)
	{
		printf("%d\n", c);
	}
	else
	{
		printf("Error\n");
		return (1);
	}
	return (0);
}
