#include <stdio.h>

/**
 * main - prints program nmae, followed by a new line
 * @argc: int
 * @argv: list
 * Retrurn: 0
 */

int main(int argc, char const *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
